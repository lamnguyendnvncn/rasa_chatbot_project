# The config recipe.
# https://rasa.com/docs/rasa/model-configuration/
recipe: default.v1

# Configuration for Rasa NLU.
# https://rasa.com/docs/rasa/nlu/components/
language: en

pipeline:
# # No configuration for the NLU pipeline was provided. The following default pipeline was used to train your model.
# # If you'd like to customize it, uncomment and adjust the pipeline.
# # See https://rasa.com/docs/rasa/tuning-your-model for more information.
  - name: WhitespaceTokenizer
  - name: RegexFeaturizer
  - name: LexicalSyntacticFeaturizer
  - name: CountVectorsFeaturizer
  - name: CountVectorsFeaturizer
    analyzer: char_wb
    min_ngram: 1
    max_ngram: 4
  - name: DIETClassifier
    epochs: 3
    constrain_similarities: true
    batch_strategy: sequence
  - name: EntitySynonymMapper
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: faq1
  - name: ResponseSelector
    epochs: 15
    retrieval_intent: events
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: benefits
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: company
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: compensation
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: culture
  - name: ResponseSelector
    epochs: 15
    retrieval_intent: development
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: olivia
  - name: ResponseSelector
    epochs: 15
    retrieval_intent: policies
  - name: ResponseSelector
    epochs: 10
    retrieval_intent: recruiting
  - name: ResponseSelector
    epochs: 15
    retrieval_intent: team

  - name: FallbackClassifier
    threshold: 0.05
    ambiguity_threshold: 0.001

# Configuration for Rasa Core.
# https://rasa.com/docs/rasa/core/policies/
policies:
# # No configuration for policies was provided. The following default policies were used to train your model.
# # If you'd like to customize them, uncomment and adjust the policies.
# # See https://rasa.com/docs/rasa/policies for more information.
  - name: MemoizationPolicy
  - name: RulePolicy
    core_fallback_threshold: 0.03
    core_fallback_action_name: action_default_fallback
    enable_fallback_prediction: true
    restrict_rules: true
    check_for_contradictions: true  
  - name: UnexpecTEDIntentPolicy
    max_history: 5
    epochs: 100
  - name: TEDPolicy
    max_history: 5
    epochs: 100
    constrain_similarities: true
